 """ 
        Created a virtual environment for the project 
            python3 -m venv venv 
                    venv2 is the name of the virtual environment 
        Packages that are installed in the environment are only local to the virtual environment
        Similar to Projects in R 
"""
print('The Phillies Rule')
print("Fuck Boston") 
print("Tom Brady the goat though")

""" 
Write a program that examines 3 variables - x,y, and z
and prints the largest odd nuber among them
"""
import random

x = random.randint(16,100)
y = random.randint(16,100)
z = random.randint(16,100)
print(x , y , z)

# Saving for later 

        